# TerraCast - Dependências do Sistema (APT)
# Observação: evitar pinagem rígida de versão para reduzir quebras entre releases Debian.
# Formato: um pacote por linha.

# Ferramentas Python essenciais
python3-pip
python3-venv

# GDAL (bibliotecas e ferramentas de build/runtime)
libgdal-dev
gdal-bin
python3-gdal

# NetCDF/HDF - bibliotecas para manipulação de dados científicos
libnetcdf-dev
libhdf5-dev
libhdf4-alt-dev

# Geoespacial - dependências para Cartopy, pyproj, shapely
libgeos-dev
libproj-dev

# Necessário para compilação do systemd-python (quando usado)
libsystemd-dev